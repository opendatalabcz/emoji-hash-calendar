services:
  backend:
    build: ./calendar-project
    env_file:
      - ./.env
    ports:
      - "5000:5000"
    depends_on:
      - db

  frontend:
    build:
      context: ./calendar-frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL}
    ports:
      - "3000:80"
    depends_on:
      - backend

  db:
    image: postgres:16
    restart: always
    env_file:
      - ./.env
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:

